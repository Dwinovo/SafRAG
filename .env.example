# === Database Configuration ===
# Default uses Docker service name 'mysql'. Change to 'localhost' if running without Docker.
DB_URL=jdbc:mysql://mysql:3306/safrag?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&createDatabaseIfNotExist=true
DB_USERNAME=root
DB_PASSWORD=root

# === JWT Configuration ===
# SECURITY WARNING: Generator a strong random string for production
JWT_SECRET=your_jwt_secret_key

# === OpenAI Configuration ===
OPENAI_BASE_URL=https://aihubmix.com
OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=gemini-2.5-flash

# === S3 / MinIO Configuration ===
# MinIO Internal: http://minio:9000
# Aliyun OSS: oss-cn-beijing.aliyuncs.com
S3_ENDPOINT=http://minio:9000
S3_PUBLIC_ENDPOINT=http://localhost:9000
S3_USERNAME=minioadmin
S3_PASSWORD=minioadmin
S3_BUCKET_NAME=safrag

# === RAG Server Configuration ===
# Docker internal: http://rag:8000
# Local standalone: http://localhost:8000
# External production: https://your-rag-server.com
RAG_SERVER_HOST=http://rag:8000

# === RAG Model Configuration ===
# Path to the embedding model (HuggingFace ID or local path)
EMBEDDING_MODEL_PATH=BAAI/bge-m3

# === Client Configuration ===
# The URL where the browser can access the backend (usually localhost for local Docker)
NEXT_PUBLIC_API_BASE=http://localhost:8080
